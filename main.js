// 主应用程序入口文件

// 导入其他模块
import './script.js';

// 主初始化函数
function initApp() {
    console.log('应用程序已初始化');
    // 在这里添加主应用程序逻辑
}

// 当DOM加载完成后初始化应用
document.addEventListener('DOMContentLoaded', initApp);